// Rewrite the code changing the for loop to while without
// altering its behavior (the output should stay same).

// for (let i = 0; i < 3; i++) {
//   window.alert(`number ${i}!`);
// }

let i = 0;
while (i < 3) {
  window.alert(`number ${i}!`);
  i++;
}
